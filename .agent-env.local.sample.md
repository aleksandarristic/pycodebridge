# Local Agent Environment

This file is gitignored. Treat it as an advisory cache only: verify relevant
commands in the current session before relying on it.

## Session

- Runtime: PyCodeBridge managed repo
- Last checked: {{CHECKED_DATE}}

## Repo Tooling

- `./.venv/bin/python`: {{PYTHON_STATUS}}
- `./.venv/bin/pip`: {{PIP_STATUS}}
- `./.venv/bin/pytest`: {{PYTEST_STATUS}}
- `./.venv/bin/ruff`: {{RUFF_VENV_STATUS}}
- Python virtualenv policy: `.venv` is present only when this repo needs
  Python tooling. When present, keep it gitignored and use `./.venv/bin/...`
  for Python tooling.

## External Tooling

- `git`: {{GIT_STATUS}}
- `gh`: {{GH_STATUS}}
- `uv`: {{UV_STATUS}}
- `python3` (system): {{PYTHON3_STATUS}}
- `ruff` (system): {{RUFF_SYSTEM_STATUS}}
- `docker`: {{DOCKER_STATUS}}
- `mise`: {{MISE_STATUS}}
- `ruby`: {{RUBY_STATUS}}
- `bundle`: {{BUNDLE_STATUS}}
- `node` / `npm`: {{NODE_NPM_STATUS}}

## Worktrees and remote hygiene

Worktrees are for local parallel work only:
- Never `git push` a worktree or session branch to remote.
- Never push any branch to remote unless the user explicitly asks.
- Never open a PR unless the user explicitly asks.
- After parallel work is merged back to the working branch, remove the
  worktree and delete its local-only branch.

### Correct worktree lifecycle

- `git worktree add` creates a local session/task worktree.
- Agent work stays local to that worktree until results are merged back.
- `git worktree remove` removes the worktree after merge-back.
- Delete the local session/task branch after cleanup; do not push it.

## Multi-agent dispatch

- Codex is the default implementation agent for focused code changes, tests,
  and repo maintenance.
- Claude is useful for planning, implementation, and review on broader or
  riskier changes.
- Gemini is useful for UI and frontend-oriented tasks.
- Trigger dispatch from Discord by naming one or more agent handles in a
  prompt, for example `!c @claude plan the auth refactor, dispatch @codex`.
- A task branch is created on first dispatch and reused for follow-up commands
  in the same task lifecycle.
- Finish the task with `!c done`. Use `!c done --merge` when the branch should
  be merged locally instead of opened as a PR.
- Keep repo intent, constraints, commands, and conventions in `AGENTS.md` so
  every agent has the same operating context.

## Notes

- Use plain `git pull` / `git push` / `git checkout` directly unless the user
  asks for a different flow.
- Re-check tools before running verification; agent runtimes can differ
  between sessions.
