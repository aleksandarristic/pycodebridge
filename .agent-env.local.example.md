# Local Agent Environment

Copy this file to `.agent-env.local.md` if a local agent runtime needs a
gitignored environment note. Treat the local file as an advisory cache: verify
relevant commands in the current session before relying on it.

## Session

- Runtime: unknown
- Last checked: YYYY-MM-DD

## Repo Tooling

- `./.venv/bin/python`: unknown
- `./.venv/bin/pip`: unknown
- `./.venv/bin/pytest`: unknown
- Python virtualenv policy: create `.venv` only when this repo needs Python
  tooling, and keep it gitignored.

## External Tooling

- `git`: unknown
- `gh`: unknown
- `docker`: unknown
- `mise`: unknown
- `ruby`: unknown
- `bundle`: unknown

## Notes

- Record runtime-specific caveats here, such as whether the current instance can
  run targeted tests, Docker checks, GitHub CLI commands, or only static checks.
